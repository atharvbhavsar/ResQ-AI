import React, { useState } from "react";

const Header = () => {
  const [tasks, settasks] = useState([
    "Dashboard",
    "My Tasks",
    "Schedule",
    "Reporting",
    "Goals",
  ]);

  return null;
};

export default Header;
